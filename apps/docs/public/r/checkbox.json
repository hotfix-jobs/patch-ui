{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "checkbox",
  "title": "Checkbox",
  "dependencies": [
    "@base-ui/react",
    "lucide-react"
  ],
  "registryDependencies": [
    "https://ui.hotfix.jobs/r/recipes.json",
    "https://ui.hotfix.jobs/r/tokens.json",
    "https://ui.hotfix.jobs/r/utils.json"
  ],
  "files": [
    {
      "path": "registry/components/ui/checkbox.tsx",
      "content": "\"use client\";\n\nimport { Checkbox as CheckboxPrimitive } from \"@base-ui/react/checkbox\";\nimport type * as React from \"react\";\nimport { Check, Minus } from \"lucide-react\";\nimport { cn } from \"@/lib/utils\";\nimport { focusRing, colorTransition } from \"@/lib/recipes\";\n\nexport interface CheckboxProps extends CheckboxPrimitive.Root.Props {\n  /** Optional label. When present, the checkbox is wrapped in a `<label>` so clicking the label toggles the box. */\n  children?: React.ReactNode;\n  /** Additional classes for the outer wrapper (only applies when `children` is provided). */\n  wrapperClassName?: string;\n}\n\nexport function Checkbox({\n  className,\n  wrapperClassName,\n  children,\n  disabled,\n  ...props\n}: CheckboxProps): React.ReactElement {\n  const box = (\n    <CheckboxPrimitive.Root\n      disabled={disabled}\n      className={cn(\n        // Unchecked box: hairline-strong border on transparent so the\n        // shape reads without overpowering the row. Hover firms the\n        // border. Checked / indeterminate flip to the --primary chain\n        // so a consumer overriding --primary gets brand-tinted checks\n        // for free.\n        \"inline-flex size-4 shrink-0 cursor-pointer items-center justify-center rounded-[var(--radius-4)]\",\n        \"bg-transparent border border-hairline-strong\",\n        \"hover:border-hairline-tertiary\",\n        \"data-checked:border-primary data-checked:bg-primary data-checked:hover:bg-primary-hover data-checked:hover:border-primary-hover\",\n        \"data-indeterminate:border-primary data-indeterminate:bg-primary\",\n        \"data-disabled:cursor-not-allowed data-disabled:opacity-50\",\n        colorTransition,\n        focusRing,\n        className,\n      )}\n      data-slot=\"checkbox\"\n      {...props}\n    >\n      <CheckboxPrimitive.Indicator\n        className=\"group flex items-center justify-center\"\n        data-slot=\"checkbox-indicator\"\n        keepMounted\n      >\n        {/* Explicit !text-on-primary on the svg itself keeps the check\n            color correct even when the checkbox lives inside a row that\n            applies an iconMuted-style descendant selector (Menu, list). */}\n        <Check\n          className=\"!text-on-primary size-3 group-data-indeterminate:hidden group-data-unchecked:hidden\"\n          strokeWidth={2.5}\n        />\n        <Minus\n          aria-hidden=\"true\"\n          className=\"!text-on-primary hidden size-3 group-data-indeterminate:block\"\n          strokeWidth={2.5}\n        />\n      </CheckboxPrimitive.Indicator>\n    </CheckboxPrimitive.Root>\n  );\n\n  if (children == null) return box;\n\n  return (\n    <label\n      className={cn(\n        \"inline-flex items-center gap-2 text-body-14 text-ink\",\n        !disabled && \"cursor-pointer\",\n        disabled && \"cursor-not-allowed opacity-50\",\n        wrapperClassName,\n      )}\n      data-slot=\"checkbox-label\"\n    >\n      {box}\n      <span>{children}</span>\n    </label>\n  );\n}\n\nexport { CheckboxPrimitive };\n",
      "type": "registry:ui",
      "target": "components/ui/checkbox.tsx"
    }
  ],
  "type": "registry:ui"
}