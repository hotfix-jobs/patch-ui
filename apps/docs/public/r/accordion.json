{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "accordion",
  "title": "Accordion",
  "dependencies": [
    "@base-ui/react",
    "lucide-react"
  ],
  "registryDependencies": [
    "https://ui.hotfix.jobs/r/recipes.json",
    "https://ui.hotfix.jobs/r/tokens.json",
    "https://ui.hotfix.jobs/r/utils.json"
  ],
  "files": [
    {
      "path": "registry/components/ui/accordion.tsx",
      "content": "\"use client\";\n\nimport { Accordion as AccordionPrimitive } from \"@base-ui/react/accordion\";\nimport { ChevronDown } from \"lucide-react\";\nimport { createContext, useContext } from \"react\";\nimport type * as React from \"react\";\nimport { cn } from \"@/lib/utils\";\nimport { focusRing, iconMuted } from \"@/lib/recipes\";\n\n/**\n * Accordion: compound built on Base UI Accordion.\n *\n * Titles read as button-14 (medium weight) and content sits in body-14.\n * Set `bordered` on the root to auto-apply the hairline row pattern to\n * every item so a stack of items reads as one continuous list. Pass\n * `multiple` on Base UI's root to allow more than one item open at once.\n *\n * Panels stay mounted for find-in-page compatibility; they collapse via\n * Base UI's `--accordion-panel-height` CSS var with a smooth transition.\n *\n * Usage:\n *   <Accordion bordered defaultValue={[\"item-1\"]}>\n *     <AccordionItem value=\"item-1\">\n *       <AccordionTrigger>How do I get started?</AccordionTrigger>\n *       <AccordionPanel>Install the package…</AccordionPanel>\n *     </AccordionItem>\n *   </Accordion>\n */\n\ntype AccordionContextValue = {\n  bordered: boolean;\n};\n\nconst AccordionContext = createContext<AccordionContextValue>({\n  bordered: false,\n});\n\nexport type AccordionProps = React.ComponentProps<typeof AccordionPrimitive.Root> & {\n  /** Auto-apply the hairline row pattern to every child item. */\n  bordered?: boolean;\n};\n\nexport function Accordion({\n  bordered = false,\n  className,\n  ...props\n}: AccordionProps): React.ReactElement {\n  return (\n    <AccordionContext.Provider value={{ bordered }}>\n      <AccordionPrimitive.Root\n        data-slot=\"accordion\"\n        data-bordered={bordered ? \"\" : undefined}\n        className={className}\n        {...props}\n      />\n    </AccordionContext.Provider>\n  );\n}\n\nexport type AccordionItemProps = React.ComponentProps<\n  typeof AccordionPrimitive.Item\n>;\n\nexport function AccordionItem({\n  className,\n  ...props\n}: AccordionItemProps): React.ReactElement {\n  const { bordered } = useContext(AccordionContext);\n  return (\n    <AccordionPrimitive.Item\n      data-slot=\"accordion-item\"\n      className={cn(\n        bordered && \"border-t border-hairline last:border-b\",\n        className,\n      )}\n      {...props}\n    />\n  );\n}\n\nexport type AccordionTriggerProps = React.ComponentProps<\n  typeof AccordionPrimitive.Trigger\n>;\n\nexport function AccordionTrigger({\n  className,\n  children,\n  ...props\n}: AccordionTriggerProps): React.ReactElement {\n  return (\n    <AccordionPrimitive.Header>\n      <AccordionPrimitive.Trigger\n        data-slot=\"accordion-trigger\"\n        className={cn(\n          // Trigger reads as a full-row button: title on the left, chevron\n          // on the right. Chevron follows the shared iconMuted recipe so\n          // it sits a step lighter than the title at rest and lifts on\n          // hover / focus / when the panel is open.\n          \"group flex w-full items-center justify-between gap-3 py-3 text-left\",\n          \"text-button-14 text-ink\",\n          iconMuted,\n          \"data-[panel-open]:[&_svg]:text-ink\",\n          focusRing,\n          className,\n        )}\n        {...props}\n      >\n        <span>{children}</span>\n        <ChevronDown\n          aria-hidden\n          className=\"size-4 shrink-0 transition-transform duration-[var(--duration-state)] ease-[var(--ease-standard)] group-data-[panel-open]:rotate-180\"\n        />\n      </AccordionPrimitive.Trigger>\n    </AccordionPrimitive.Header>\n  );\n}\n\nexport type AccordionPanelProps = React.ComponentProps<\n  typeof AccordionPrimitive.Panel\n>;\n\nexport function AccordionPanel({\n  className,\n  children,\n  ...props\n}: AccordionPanelProps): React.ReactElement {\n  return (\n    <AccordionPrimitive.Panel\n      data-slot=\"accordion-panel\"\n      keepMounted\n      className={cn(\n        \"h-[var(--accordion-panel-height)] overflow-hidden\",\n        \"transition-[height] duration-[var(--duration-overlay)] ease-[var(--ease-standard)]\",\n        \"data-[starting-style]:h-0 data-[ending-style]:h-0\",\n        className,\n      )}\n      {...props}\n    >\n      <div className=\"pb-3 text-body-14 text-ink-muted\">\n        {children}\n      </div>\n    </AccordionPrimitive.Panel>\n  );\n}\n\nexport { AccordionPrimitive };\n",
      "type": "registry:ui",
      "target": "components/ui/accordion.tsx"
    }
  ],
  "type": "registry:ui"
}