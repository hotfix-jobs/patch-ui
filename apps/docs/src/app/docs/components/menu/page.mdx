import { MenuDemo } from "./menu-demo";


export const metadata = { title: "Menu" };

# Menu

A dropdown menu with support for items, groups, checkbox items, radio items, sub-menus, keyboard shortcuts, and separators. Custom-built on `@floating-ui/react` with `motion`-driven spring open/close, `FloatingTree` for sub-menu coordination, list navigation, and type-ahead.

<Install name="menu" />

<ComponentPreview label="Menu Examples">
  <MenuDemo />
</ComponentPreview>

## Usage

```tsx
import {
  Button,
  Menu,
  MenuTrigger,
  MenuPopup,
  MenuItem,
  MenuGroup,
  MenuGroupLabel,
  MenuSection,
  MenuCheckboxItem,
  MenuRadioGroup,
  MenuRadioItem,
  MenuSeparator,
  MenuShortcut,
  MenuSub,
  MenuSubTrigger,
  MenuSubPopup,
} from "@patchui/react";

// Basic menu
<Menu>
  <MenuTrigger render={<Button />}>Open Menu</MenuTrigger>
  <MenuPopup>
    <MenuItem>Profile</MenuItem>
    <MenuItem>Settings</MenuItem>
    <MenuSeparator />
    <MenuItem variant="destructive">Log out</MenuItem>
  </MenuPopup>
</Menu>

// Single-select with a trailing check
const [sortBy, setSortBy] = useState("newest");
<MenuItem selected={sortBy === "newest"} onClick={() => setSortBy("newest")}>
  Newest first
</MenuItem>

// Two-line items
<MenuItem description="Search across the workspace">
  Search
  <MenuShortcut>⌘K</MenuShortcut>
</MenuItem>

// Sections (sugar for MenuGroup + MenuGroupLabel)
<MenuSection label="Account">
  <MenuItem>Profile</MenuItem>
  <MenuItem>Settings</MenuItem>
</MenuSection>

// Comfortable density (default is compact)
<MenuPopup density="comfortable">...</MenuPopup>

// With checkbox items
const [checked, setChecked] = useState(true);
<MenuCheckboxItem checked={checked} onCheckedChange={setChecked}>
  Show Sidebar
</MenuCheckboxItem>

// With radio items
const [value, setValue] = useState("system");
<MenuRadioGroup value={value} onValueChange={setValue}>
  <MenuRadioItem value="light">Light</MenuRadioItem>
  <MenuRadioItem value="dark">Dark</MenuRadioItem>
  <MenuRadioItem value="system">System</MenuRadioItem>
</MenuRadioGroup>
```

## Props

### MenuItem

<PropsTable
  props={[
    {
      name: "variant",
      type: '"default" | "destructive"',
      default: '"default"',
      description: "Visual variant. Use destructive for dangerous actions like delete or log out.",
    },
    {
      name: "selected",
      type: "boolean",
      default: "false",
      description: "Renders a trailing check to indicate the currently chosen item in a single-select list. Distinct from MenuCheckboxItem, which is purpose-built for multi-select toggling with a leading indicator.",
    },
    {
      name: "description",
      type: "ReactNode",
      description: "Secondary line shown beneath the title for two-line items. Useful for command palettes or settings menus that benefit from a one-line hint.",
    },
    {
      name: "inset",
      type: "boolean",
      default: "false",
      description: "Adds left padding to align with items that have icons or indicators.",
    },
    {
      name: "disabled",
      type: "boolean",
      default: "false",
      description: "Disables the menu item.",
    },
  ]}
/>

### MenuPopup

<PropsTable
  props={[
    {
      name: "density",
      type: '"compact" | "comfortable"',
      default: '"compact"',
      description: "Vertical density of contained items. Compact targets dense filter and action menus; comfortable bumps min-height and padding for marketing or settings menus.",
    },
    {
      name: "side",
      type: '"top" | "bottom" | "left" | "right"',
      default: '"bottom"',
      description: "Which side of the trigger the popup appears on.",
    },
    {
      name: "sideOffset",
      type: "number",
      default: "4",
      description: "Distance in pixels between the popup and trigger.",
    },
    {
      name: "align",
      type: '"start" | "center" | "end"',
      default: '"center"',
      description: "Alignment of the popup relative to the trigger.",
    },
  ]}
/>

## Compound Components

<CompoundComponents
  components={[
    { name: "Menu", description: "Root component managing open/close state." },
    { name: "MenuTrigger", description: "Element that opens the menu on click." },
    { name: "MenuPopup", description: "Portal-rendered popup with positioner." },
    { name: "MenuItem", description: "A single menu action. Supports variant, selected, description, and inset." },
    { name: "MenuGroup", description: "Groups related items together." },
    { name: "MenuGroupLabel", description: "Label for a group. Supports inset." },
    { name: "MenuSection", description: "Compound section: a labeled group with consistent vertical spacing. Sugar for MenuGroup + MenuGroupLabel." },
    { name: "MenuCheckboxItem", description: "A togglable item with a check indicator." },
    { name: "MenuRadioGroup", description: "Container for mutually exclusive radio items." },
    { name: "MenuRadioItem", description: "A radio option within a group." },
    { name: "MenuSeparator", description: "A visual divider between sections." },
    { name: "MenuShortcut", description: "Displays a keyboard shortcut hint, right-aligned." },
    { name: "MenuSub", description: "Wrapper for a sub-menu." },
    { name: "MenuSubTrigger", description: "Opens the sub-menu on hover. Supports inset." },
    { name: "MenuSubPopup", description: "Portal-rendered popup for the sub-menu." },
  ]}
/>


<Callout title="Notes">
- **Keyboard navigation:** Supports arrow keys, type-ahead, Home/End, Enter/Space, and Escape.
- **Sub-menus:** Nest `MenuSub` > `MenuSubTrigger` + `MenuSubPopup` for cascading menus. Sub-menus open on hover. The sub-popup inherits the parent `MenuPopup`'s density unless overridden.
- **Portal rendering:** The popup renders outside the DOM tree via a portal, so it appears above all other content.
- **Inset alignment:** Use the `inset` prop on `MenuItem`, `MenuGroupLabel`, and `MenuSubTrigger` to align text with items that have icons or check indicators.
- **Motion:** Open AND close are spring-animated via `motion` + `AnimatePresence` (true bidirectional, not CSS-driven). Stiffness 400, damping 30, mass 0.6. Respects `prefers-reduced-motion`.
- **Foundation:** Built on `@floating-ui/react`: `useFloating` for positioning, `useListNavigation` for arrow keys, `useTypeahead` for type-to-jump, `FloatingTree` so nested sub-menus coordinate dismissal cleanly.
- **`selected` vs `MenuCheckboxItem`:** Use `selected` for single-select "currently chosen" UI with a trailing check. Use `MenuCheckboxItem` for multi-select toggles with a leading indicator.
</Callout>
