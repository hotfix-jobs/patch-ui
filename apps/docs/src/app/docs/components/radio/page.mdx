import { RadioDemo } from "./radio-demo";
import { RadioStatesDemo } from "./radio-states-demo";

export const metadata = {
  title: "Radio",
  alternates: { canonical: "/docs/components/radio" },
};

export const usageCode = `<RadioGroup aria-label="Billing cycle" defaultValue="monthly">
  <Radio value="monthly">Monthly</Radio>
  <Radio value="yearly">Yearly</Radio>
</RadioGroup>`;

<PageHeader
  title="Radio"
  description="A grouped control for choosing exactly one option from a small visible set."
/>

Use Radio when seeing every option helps comparison and only one may be selected. Use Select or Combobox for longer lists.

<ComponentPreview label="Plan selection" sourcePath="src/app/docs/components/radio/radio-demo.tsx">
  <RadioDemo />
</ComponentPreview>

## Installation

<Install name="radio" />

The copied source is available in the [registry JSON](https://ui.hotfix.jobs/r/radio.json). The canonical implementation lives in [packages/react/src/components/radio.tsx](https://github.com/hotfix-jobs/patch-ui/blob/main/packages/react/src/components/radio.tsx).

## Usage

<Usage code={usageCode} />

Pass label text as Radio children to make the complete row selectable. Every value must be unique within its RadioGroup.

## Composition

```text
RadioGroup
└── Radio
```

## Examples

### Unavailable option

Disable an individual Radio when the option remains useful context but cannot currently be selected. Disable the group when no choices are available.

<ComponentPreview label="Unavailable option" sourcePath="src/app/docs/components/radio/radio-states-demo.tsx">
  <RadioStatesDemo />
</ComponentPreview>

## API reference

<PropsTable props={[
  { name: "RadioGroup.value / onValueChange", type: "string / (value, details) => void", description: "Controls the selected value." },
  { name: "RadioGroup.defaultValue", type: "string", description: "Sets the initial uncontrolled selection." },
  { name: "RadioGroup.name", type: "string", description: "Names the submitted form value." },
  { name: "RadioGroup.required", type: "boolean", default: "false", description: "Requires one option before form submission." },
  { name: "RadioGroup.disabled", type: "boolean", default: "false", description: "Disables every option in the group." },
  { name: "Radio.value", type: "string", description: "Identifies the option within its group." },
  { name: "Radio.children", type: "ReactNode", description: "Adds a visible label and expands the selectable target." },
]} />

Radio also accepts `disabled` for one unavailable option. Bare radios are supported for custom layouts but require a specific accessible name.

## Accessibility

- Give every RadioGroup an accessible name with visible labeling through `aria-labelledby` or a concise `aria-label`.
- Use Radio children for visible option labels. Do not rely on color or position alone.
- Tab enters or leaves the group. Arrow keys move selection between enabled options.
- Preselect the safest reasonable default. Omit a default when the decision must be deliberate.
- Keep the visible list small enough to compare without excessive keyboard movement.

<PageNavigation />
