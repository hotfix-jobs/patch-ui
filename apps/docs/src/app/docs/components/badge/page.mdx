import { BadgeDemo } from "./badge-demo";

# Badge

A small status indicator for labels, counts, and tags. Five semantic variants, two sizes, two shapes, and a dismissible pattern for filter chips.

<Install name="badge" />

<ComponentPreview label="Badge Examples">
  <BadgeDemo />
</ComponentPreview>

## Usage

```tsx
import { Badge } from "@patchui/react";

// Basic
<Badge>Default</Badge>

// Semantic
<Badge variant="success">Active</Badge>
<Badge variant="warning">Pending</Badge>
<Badge variant="error">Failed</Badge>

// Sizes
<Badge size="sm">Small</Badge>
<Badge size="lg">Large</Badge>

// Shape
<Badge shape="rounded">Rounded</Badge>
<Badge shape="pill">Pill</Badge>

// As a link
<Badge render={<a href="/status" />}>Linked</Badge>

// Dismissible — trailing × for filter chips, tokens, facets
<Badge variant="secondary" onRemove={() => removeFilter("Design")}>
  Design
</Badge>
```

## Props

<PropsTable
  props={[
    {
      name: "variant",
      type: '"default" | "secondary" | "success" | "warning" | "error"',
      default: '"default"',
      description: "The visual style. Each variant uses a `-100` fill with a `-900` text token from the matching hue scale.",
    },
    {
      name: "size",
      type: '"sm" | "lg"',
      default: '"sm"',
      description: "Badge size. `lg` gets a slightly larger padding block.",
    },
    {
      name: "shape",
      type: '"rounded" | "pill"',
      default: '"rounded"',
      description: "`rounded` = 6px radius, `pill` = fully rounded.",
    },
    {
      name: "onRemove",
      type: "() => void",
      default: "-",
      description: "When provided, renders a trailing × button that calls this handler. Turns the badge into a removable chip.",
    },
    {
      name: "removeLabel",
      type: "string",
      default: '"Remove"',
      description: "Accessible label for the × button.",
    },
    {
      name: "render",
      type: "RenderProp<'span'>",
      default: "-",
      description: "Polymorphic render prop. Pass a different element (e.g. `<a>`) to change the rendered tag while keeping badge styles.",
    },
    {
      name: "className",
      type: "string",
      default: "-",
      description: "Additional CSS classes.",
    },
  ]}
/>

<Callout title="Notes">
- **Contract:** every variant uses a hue-100 fill + hue-900 text. Contrast is verified in `check:contrast` at build time.
- **Polymorphic:** `render={<a href="…" />}` renders as an anchor while keeping badge styles.
- **Dismissible:** the × sits at 50% opacity, fades to 80% on badge hover, 100% on × hover.
- **Solid accent reserved:** stick to `success` / `warning` / `error` for actual state; don't reach for them decoratively.
</Callout>
