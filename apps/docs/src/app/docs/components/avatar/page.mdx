import { AvatarDemo } from "./avatar-demo";
import { AvatarSizesDemo } from "./avatar-sizes-demo";
import { AvatarGroupDemo } from "./avatar-group-demo";

export const metadata = {
  title: "Avatar",
  alternates: { canonical: "/docs/components/avatar" },
};

export const usageCode = `<Avatar>
  <AvatarImage src="/people/ada.jpg" alt="Ada Lovelace" />
  <AvatarFallback>AL</AvatarFallback>
</Avatar>`;

<PageHeader
  title="Avatar"
  description="A visual identity for a person or team with image, monogram, and placeholder fallbacks."
/>

Use Avatar to reinforce an identity that is already available in nearby text or an accessible image label. Use AvatarGroup for a compact summary of multiple members.

<ComponentPreview label="Fallback initials" sourcePath="src/app/docs/components/avatar/avatar-demo.tsx">
  <AvatarDemo />
</ComponentPreview>

## Installation

<Install name="avatar" />

The copied source is available in the [registry JSON](https://ui.hotfix.jobs/r/avatar.json). The canonical implementation lives in [packages/react/src/components/avatar.tsx](https://github.com/hotfix-jobs/patch-ui/blob/main/packages/react/src/components/avatar.tsx).

## Usage

<Usage code={usageCode} />

Always provide fallback initials when an image may fail to load. The `letter` prop creates a deterministic monogram without requiring an image request.

## Composition

```text
Avatar
├── AvatarImage
└── AvatarFallback

AvatarGroup
└── Avatar or members[]
```

## Examples

### Size and shape

Prefer the named size scale. Numeric pixel sizes are available when Avatar must align to fixed external geometry.

<ComponentPreview label="Size and shape" sourcePath="src/app/docs/components/avatar/avatar-sizes-demo.tsx">
  <AvatarSizesDemo />
</ComponentPreview>

### Group and overflow

Pass `members` for data-driven lists. Use children when individual avatars need custom content or behavior.

<ComponentPreview label="Group and overflow" sourcePath="src/app/docs/components/avatar/avatar-group-demo.tsx">
  <AvatarGroupDemo />
</ComponentPreview>

## API reference

### Avatar

<PropsTable props={[
  { name: "size", type: '"xs" | "sm" | "md" | "lg" | "xl" | number', default: '"md"', description: "Sets a 24, 28, 36, 44, or 56 pixel preset, or an exact pixel size." },
  { name: "shape", type: '"circle" | "square"', default: '"circle"', description: "Selects a circular or radius-6 container." },
  { name: "letter", type: "string", description: "Adds a monogram fallback without composing AvatarFallback." },
  { name: "placeholder", type: "boolean", default: "false", description: "Uses a neutral fill and renders a person icon when no other body is supplied." },
  { name: "icon / iconBackground", type: "ReactNode / boolean", default: "- / true", description: "Adds a small badge at the lower edge and selects its fill treatment." },
]} />

### AvatarGroup

<PropsTable props={[
  { name: "members", type: "AvatarGroupMember[]", description: "Renders a data-driven collection as an alternative to children." },
  { name: "limit", type: "number", default: "5", description: "Limits visible members and adds a +N summary. Set 0 to disable the limit." },
  { name: "size / shape", type: "AvatarSize / AvatarShape", default: '"md" / "circle"', description: "Applies shared presentation to members and the overflow summary." },
  { name: "overlap", type: '"auto" | number', default: '"auto"', description: "Scales overlap with avatar size or uses an exact pixel value." },
  { name: "reverse", type: "boolean", default: "false", description: "Reverses stacking order without changing visual member order." },
]} />

## Accessibility

- Use meaningful `alt` text when the avatar image communicates identity. Use an empty `alt` when adjacent text already names the same person.
- Do not rely on initials, color, or a status badge as the only source of important information.
- Avatar is display-only. Wrap it in an appropriately named Button or link when it initiates an action.
- Preserve stable member order when using AvatarGroup. Visual overlap must not imply a ranking that the data does not have.

<PageNavigation />
