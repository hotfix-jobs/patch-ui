import { AccordionDemo } from "./accordion-demo";
import { AccordionVariantsDemo } from "./accordion-variants-demo";
import { AccordionCaretDemo } from "./accordion-caret-demo";

export const metadata = {
  title: "Accordion",
  alternates: { canonical: "/docs/components/accordion" },
};

export const usageCode = `<Accordion defaultValue={["item-1"]}>
  <AccordionItem value="item-1">
    <AccordionTrigger>Can multiple sections be open?</AccordionTrigger>
    <AccordionPanel>
      <p className="pb-3 text-small text-ink-muted">
        Yes. Accordion allows multiple open items by default.
      </p>
    </AccordionPanel>
  </AccordionItem>
</Accordion>`;

<PageHeader
  title="Accordion"
  description="A coordinated set of collapsible sections for progressively revealing content."
/>

Use Accordion for related disclosures such as product details or frequently asked questions. A single item also supports a compact show-more pattern.

<ComponentPreview label="Frequently asked questions" sourcePath="src/app/docs/components/accordion/accordion-demo.tsx">
  <AccordionDemo />
</ComponentPreview>

## Installation

<Install name="accordion" />

The copied source is available in the [registry JSON](https://ui.hotfix.jobs/r/accordion.json). The canonical implementation lives in [packages/react/src/components/accordion.tsx](https://github.com/hotfix-jobs/patch-ui/blob/main/packages/react/src/components/accordion.tsx).

## Usage

<Usage code={usageCode} />

Each item needs a unique `value`. Panel content stays mounted while collapsed, which preserves find-in-page behavior.

## Composition

```text
Accordion
└── AccordionItem
    ├── AccordionTrigger
    └── AccordionPanel
```

## Examples

### Visual treatments

Use `flush` for content integrated into a page, `bordered` for a divided list, and `card` when each disclosure is a distinct content object.

<ComponentPreview label="Visual treatments" sourcePath="src/app/docs/components/accordion/accordion-variants-demo.tsx">
  <AccordionVariantsDemo />
</ComponentPreview>

### Custom indicator

Replace the default caret with any node, or pass `null` when another visual treatment communicates disclosure state.

<ComponentPreview label="Custom indicator" sourcePath="src/app/docs/components/accordion/accordion-caret-demo.tsx">
  <AccordionCaretDemo />
</ComponentPreview>

## API reference

<PropsTable props={[
  { name: "variant", type: '"flush" | "bordered" | "card"', default: '"flush"', description: "Selects integrated, divided, or discrete-card presentation." },
  { name: "defaultValue", type: "string[]", default: "[]", description: "Sets initially expanded items for uncontrolled state." },
  { name: "value / onValueChange", type: "string[] / (value: string[]) => void", description: "Controls the expanded item values." },
  { name: "openMultiple", type: "boolean", default: "true", description: "Allows multiple panels to remain open. Set false for exclusive expansion." },
]} />

Every AccordionItem needs a unique `value`. AccordionTrigger renders a `CaretDown` by default; pass another node to `caret`, or `null` to hide it.

## Accessibility

- Triggers use buttons and expose expanded state through the underlying Base UI primitive.
- Enter or Space toggles the focused item. Arrow keys move between triggers, while Home and End move to the first or last trigger.
- Keep trigger labels concise and make panel content understandable when reached directly through find-in-page.
- Use `openMultiple={false}` only when simultaneous comparison is unnecessary.

<PageNavigation />
