import { CardDemo } from "./card-demo";
import { CardVariantsDemo } from "./card-variants-demo";
import { CardActionableDemo } from "./card-actionable-demo";

export const metadata = {
  title: "Card",
  alternates: { canonical: "/docs/components/card" },
};

export const usageCode = `<Card>
  <CardHeader>
    <div>
      <CardTitle>Workspace activity</CardTitle>
      <CardDescription>Changes from the last seven days.</CardDescription>
    </div>
  </CardHeader>
  <CardContent>Three updates were published.</CardContent>
</Card>`;

<PageHeader
  title="Card"
  description="A composable surface for a discrete content object with optional header, content, and footer structure."
/>

Use Card when content represents one object that benefits from a visible boundary or elevation. Use Section for a borderless group of related settings or controls.

<ComponentPreview label="Structured card" sourcePath="src/app/docs/components/card/card-demo.tsx" previewClassName="bg-base">
  <CardDemo />
</ComponentPreview>

## Installation

<Install name="card" />

The copied source is available in the [registry JSON](https://ui.hotfix.jobs/r/card.json). The canonical implementation lives in [packages/react/src/components/card.tsx](https://github.com/hotfix-jobs/patch-ui/blob/main/packages/react/src/components/card.tsx).

## Usage

<Usage code={usageCode} />

Card defaults to `variant="outlined"`. Its structural parts are independent, so omit any slot the content does not need.

## Composition

```text
Card
├── CardHeader
│   ├── CardTitle
│   └── CardDescription
├── CardContent
└── CardFooter
```

## Examples

### Surface variants

Use outlined for a clear object boundary, surface for quiet grouping, and elevated when an object must sit above surrounding content.

<ComponentPreview label="Surface variants" sourcePath="src/app/docs/components/card/card-variants-demo.tsx" previewClassName="bg-base">
  <CardVariantsDemo />
</ComponentPreview>

### Actionable card

`actionable` adds visual interaction states only. Render the complete Card as a link or button to supply the correct semantics.

<ComponentPreview label="Actionable card" sourcePath="src/app/docs/components/card/card-actionable-demo.tsx" previewClassName="bg-base">
  <CardActionableDemo />
</ComponentPreview>

## API reference

<PropsTable props={[
  { name: "variant", type: '"surface" | "outlined" | "elevated"', default: '"outlined"', description: "Selects quiet, bounded, or elevated presentation." },
  { name: "actionable", type: "boolean", default: "false", description: "Adds hover, active, disabled, and keyboard-focus presentation for a semantic link or button." },
  { name: "selected", type: "boolean", default: "false", description: "Adds selected presentation and data-selected without choosing an ARIA selection model." },
  { name: "render", type: "RenderProp<\"div\">", description: "Changes the semantic root while retaining Card presentation." },
]} />

All structural parts support `render` when their semantic element needs to change. CardFooter adds a top boundary with `divided`.

## Accessibility

- A plain Card is a visual grouping and does not add landmark semantics automatically.
- Choose a CardTitle heading level that fits the surrounding document hierarchy.
- When the whole card is interactive, render it as one link or button. Do not nest other interactive controls inside it.
- `selected` is visual state only. Add `aria-selected`, `aria-pressed`, or another state required by the parent interaction pattern.
- Keep a visible text label when an icon appears in the header or footer as an action.

<PageNavigation />
