import { CardDemo } from "./card-demo";

# Card

A compound container component with multiple visual variants for grouping related content.

<Install name="card" />

<div className="my-6">
  <CardDemo />
</div>

## Usage

```tsx
import {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardAction,
  CardContent,
  CardFooter,
  CardMeta,
  Button,
} from "@patchui/react";

// Basic card with header, content, and footer
<Card>
  <CardHeader>
    <CardTitle>Project Settings</CardTitle>
    <CardDescription>Manage your configuration.</CardDescription>
  </CardHeader>
  <CardContent>
    <p>Your content here.</p>
  </CardContent>
  <CardFooter>
    <Button size="sm">Save</Button>
  </CardFooter>
</Card>

// With a header action
<Card>
  <CardHeader>
    <CardTitle>Notifications</CardTitle>
    <CardDescription>3 unread messages.</CardDescription>
    <CardAction>
      <Button variant="secondary" size="sm">Mark all read</Button>
    </CardAction>
  </CardHeader>
</Card>

// Interactive variant (hover + press feedback)
<Card variant="interactive">
  <CardContent>Hover me</CardContent>
</Card>

// Editorial variant (flat, no radius — for gallery / grid layouts)
<Card variant="editorial" className="px-6 py-10">
  <CardContent>Flat editorial chrome</CardContent>
</Card>

// Selected — stronger border for selectable card grids
<Card variant="interactive" selected={isSelected}>
  ...
</Card>

// With meta footer (person attribution)
<Card>
  <CardHeader>
    <CardTitle>Senior Engineer</CardTitle>
    <CardDescription>Platform team · Remote</CardDescription>
  </CardHeader>
  <CardContent>
    <p>Referred by a team member.</p>
  </CardContent>
  <CardMeta
    icon={<span className="inline-flex size-7 items-center justify-center rounded-[var(--radius-patch-sm)] bg-patch-surface-hover text-[11px] font-semibold text-patch-text-secondary">JD</span>}
    primary="Jane Doe"
    secondary="Staff Engineer"
    action={<Button size="sm">Apply</Button>}
  />
</Card>
```

## Props

<PropsTable
  props={[
    {
      name: "variant",
      type: '"default" | "elevated" | "interactive" | "ghost" | "editorial"',
      default: '"default"',
      description: "The visual style. default/elevated/interactive/ghost are 12px-rounded surfaces. editorial is flat (no radius) with a hairline border — suitable for gallery/grid layouts where the rounded corners would otherwise fight the layout.",
    },
    {
      name: "selected",
      type: "boolean",
      default: "false",
      description: "Switches the border to --patch-text for a stronger highlight. Use in selectable card grids (multi-pick galleries, job picker, plan comparison). Composes with any variant.",
    },
    {
      name: "render",
      type: "RenderProp<'div'>",
      default: "-",
      description: "Polymorphic render prop - pass a different element to change the rendered tag.",
    },
    {
      name: "className",
      type: "string",
      default: "-",
      description: "Additional CSS classes to apply.",
    },
    {
      name: "children",
      type: "ReactNode",
      default: "-",
      description: "The card content - typically composed of CardHeader, CardContent, and CardFooter.",
    },
  ]}
/>


<Callout title="Notes">
- **Compound component:** Card is built from composable sub-components: `CardHeader`, `CardTitle`, `CardDescription`, `CardAction`, `CardContent`, and `CardFooter`. Use them in any combination.
- **CardAction:** Place `CardAction` inside `CardHeader` to render an action button in the top-right corner. The header uses CSS grid to position it automatically.
- **Polymorphic:** Card and all sub-components accept a `render` prop (via Base UI's `useRender`) to change the rendered element. For example, `render={<a href="/detail" />}` renders the card as a link.
- **Interactive variant:** Use `variant="interactive"` for clickable cards. Hover changes bg + border tone; an `active:scale-[0.99]` press provides tactile feedback.
- **Editorial variant:** Flat surface (no radius). Pair with explicit `className` padding (`px-6 py-10`, `px-5 py-4`, etc.) for editorial chrome — replaces the need for a custom Cell wrapper.
- **Selected state:** `selected={true}` switches the border to `--patch-text` and sets `data-selected` + `aria-selected` for both styling and a11y. Combine with any variant.
- **CardMeta:** A layout primitive for a footer attribution row — icon, primary + optional secondary text, optional trailing action. Renders a hairline border-top automatically. Works for person attribution, system attribution, or any similar pattern. `primary` and `secondary` accept any `ReactNode`.
</Callout>
