import { SectionLabelDemo } from "./section-label-demo";

# SectionLabel

A small uppercase label for introducing a section of content, with an optional trailing action. Two variants: `bare` (just the label) and `divided` (adds a hairline rule beneath).

<Install name="section-label" />

<ComponentPreview label="SectionLabel Example">
  <SectionLabelDemo />
</ComponentPreview>

## Usage

```tsx
import { SectionLabel } from "@patchui/react";

<SectionLabel>Overview</SectionLabel>

<SectionLabel variant="divided" action="View all">
  Recent activity
</SectionLabel>
```

## Props

<PropsTable
  props={[
    {
      name: "variant",
      type: '"bare" | "divided"',
      default: '"bare"',
      description: "bare is just the label; divided adds a hairline rule and more bottom spacing.",
    },
    {
      name: "action",
      type: "ReactNode",
      default: "-",
      description: "Optional trailing content (e.g. a link), rendered in normal case to the right of the label.",
    },
    {
      name: "render",
      type: "RenderProp<'div'>",
      default: "-",
      description: "Polymorphic render prop to change the rendered element while keeping the label styling.",
    },
    {
      name: "className",
      type: "string",
      default: "-",
      description: "Additional CSS classes applied to the label.",
    },
  ]}
/>

<Callout title="Notes">
- **Pairing:** Use above lists, cards, or form groups to label a region without the weight of a full heading.
- **Action:** The `action` slot is right-aligned and rendered in normal case, ideal for a "View all" link or count.
</Callout>
