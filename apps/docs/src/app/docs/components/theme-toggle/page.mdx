import { ThemeToggleControlledDemo } from "./theme-toggle-controlled-demo";
import { ThemeToggleDemo } from "./theme-toggle-demo";
import { ThemeToggleSizesDemo } from "./theme-toggle-sizes-demo";

export const metadata = {
  title: "Theme Toggle",
  alternates: { canonical: "/docs/components/theme-toggle" },
};

export const usageCode = `<ThemeToggle />`;

<PageHeader
  title="Theme Toggle"
  description="An icon button that switches between light and dark color themes."
/>

Use Theme Toggle for a compact theme control in a header, sidebar, or settings surface. It can manage the document theme itself or integrate with an existing theme provider.

<ComponentPreview label="Theme toggle" sourcePath="src/app/docs/components/theme-toggle/theme-toggle-demo.tsx">
  <ThemeToggleDemo />
</ComponentPreview>

## Installation

<Install name="theme-toggle" />

The copied source is available in the [registry JSON](https://ui.hotfix.jobs/r/theme-toggle.json). The canonical implementation lives in [packages/react/src/components/theme-toggle.tsx](https://github.com/hotfix-jobs/patch-ui/blob/main/packages/react/src/components/theme-toggle.tsx).

## Usage

<Usage code={usageCode} />

In uncontrolled mode, Theme Toggle reads the saved theme or system preference, updates the `.dark` class on the document, and persists later changes to local storage.

## Examples

### Controlled

Control the theme when an application already owns theme state. Disable class application and storage when a provider handles those responsibilities.

<ComponentPreview label="Controlled theme" sourcePath="src/app/docs/components/theme-toggle/theme-toggle-controlled-demo.tsx">
  <ThemeToggleControlledDemo />
</ComponentPreview>

### Sizes

Choose the size that matches the surrounding controls.

<ComponentPreview label="Theme toggle sizes" sourcePath="src/app/docs/components/theme-toggle/theme-toggle-sizes-demo.tsx">
  <ThemeToggleSizesDemo />
</ComponentPreview>

## Prevent theme flash

Add a small inline script to the document head so the saved or system theme is applied before the page paints.

```html
<script>
  (function () {
    var theme = localStorage.getItem("patch-ui-theme");
    if (!theme) {
      theme = matchMedia("(prefers-color-scheme: dark)").matches
        ? "dark"
        : "light";
    }
    if (theme === "dark") document.documentElement.classList.add("dark");
  })();
</script>
```

## API reference

<PropsTable props={[
  { name: "theme", type: '"light" | "dark" | "system"', description: "Controls the requested theme. Omit for uncontrolled behavior." },
  { name: "onThemeChange", type: "(theme: ResolvedTheme) => void", description: "Runs with the resolved light or dark theme after activation." },
  { name: "storageKey", type: "string | false", default: '"patch-ui-theme"', description: "Sets the local storage key, or disables persistence." },
  { name: "applyClass", type: "boolean", default: "true", description: "Controls whether the component updates the document’s dark class." },
  { name: "size", type: '"sm" | "md" | "lg"', default: '"md"', description: "Matches the shared control size scale." },
]} />

## Accessibility

- The button label announces the theme that activation will switch to.
- The icon is decorative and does not replace the accessible button name.
- Theme changes should preserve readable contrast in both modes.
- Icon motion is removed when reduced motion is preferred.

<PageNavigation />
