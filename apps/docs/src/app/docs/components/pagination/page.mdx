import { PaginationDemo } from "./pagination-demo";
import { PaginationStatesDemo } from "./pagination-states-demo";

export const metadata = {
  title: "Pagination",
  alternates: { canonical: "/docs/components/pagination" },
};

export const usageCode = `const [page, setPage] = useState(1);

<Pagination
  page={page}
  totalPages={12}
  onPageChange={setPage}
/>`;

<PageHeader
  title="Pagination"
  description="A controlled page range with previous, next, current-page, and ellipsis navigation."
/>

Use Pagination to divide a large ordered collection into addressable pages. Prefer a single continuous view when the complete collection remains manageable.

<ComponentPreview label="Interactive pagination" sourcePath="src/app/docs/components/pagination/pagination-demo.tsx">
  <PaginationDemo />
</ComponentPreview>

## Installation

<Install name="pagination" />

The copied source is available in the [registry JSON](https://ui.hotfix.jobs/r/pagination.json). The canonical implementation lives in [packages/react/src/components/pagination.tsx](https://github.com/hotfix-jobs/patch-ui/blob/main/packages/react/src/components/pagination.tsx).

## Usage

<Usage code={usageCode} />

`page` is one-indexed and controlled by the application. Pagination renders nothing when `totalPages` is one or less.

## Examples

### Long range and loading

Long ranges retain the first and last page and collapse gaps into ellipses. Loading disables every control and marks the navigation landmark busy.

<ComponentPreview label="Long range and loading" sourcePath="src/app/docs/components/pagination/pagination-states-demo.tsx">
  <PaginationStatesDemo />
</ComponentPreview>

## API reference

<PropsTable props={[
  { name: "page", type: "number", description: "Sets the current one-indexed page." },
  { name: "totalPages", type: "number", description: "Sets the total number of available pages." },
  { name: "onPageChange", type: "(page: number) => void", description: "Handles client-side navigation when href is absent." },
  { name: "href", type: "(page: number) => string", description: "Renders page controls as crawlable anchors using generated URLs." },
  { name: "siblingCount", type: "number", default: "1", description: "Sets how many page numbers remain visible on each side of the current page." },
  { name: "loading", type: "boolean", default: "false", description: "Disables navigation and exposes aria-busy during a page transition." },
]} />

## Indexable pages

Use `href` for catalogs, directories, archives, and other server-rendered collections. Return the canonical bare route for page one and a stable page URL for every later page. Use `onPageChange` for client-only views where the URL does not represent an indexable collection.

## Accessibility

- Pagination renders a navigation landmark named `Pagination`.
- The current page receives `aria-current="page"`.
- Previous, next, and numeric controls have specific accessible names.
- Tab moves between controls. Enter activates links, while Enter or Space activates buttons.
- Arrow keys are intentionally unbound, so Pagination remains compatible with horizontal scrolling containers.

<PageNavigation />
