import { CheckboxDemo } from "./checkbox-demo";
import { CheckboxStatesDemo } from "./checkbox-states-demo";

export const metadata = { title: "Checkbox" };
export const usageCode = `<Checkbox>Send weekly digest</Checkbox>`;

<PageHeader title="Checkbox" description="A checked, unchecked, or indeterminate control for selection and acknowledgment." />

Use Checkbox when people can select zero or more options or affirm a statement. Use Switch for a setting that takes effect immediately.

<ComponentPreview label="Checkbox group" sourcePath="src/app/docs/components/checkbox/checkbox-demo.tsx"><CheckboxDemo /></ComponentPreview>

## Installation

<Install name="checkbox" />

The copied source is available in the [registry JSON](https://ui.hotfix.jobs/r/checkbox.json). The canonical implementation lives in [packages/react/src/components/checkbox.tsx](https://github.com/hotfix-jobs/patch-ui/blob/main/packages/react/src/components/checkbox.tsx).

## Usage

<Usage code={usageCode} />

Passing children creates a clickable label. Bare checkboxes require an accessible name.

## Examples

### Selection states

Indeterminate communicates partial collection selection. Application state still determines what happens on the next change.

<ComponentPreview label="Selection states" sourcePath="src/app/docs/components/checkbox/checkbox-states-demo.tsx"><CheckboxStatesDemo /></ComponentPreview>

## API reference

Checkbox accepts the underlying Base UI checkbox props. These fields coordinate Patch UI behavior.

<PropsTable props={[
  { name: "children", type: "ReactNode", description: "Adds a visible label and expands the click target." },
  { name: "checked / onCheckedChange", type: "boolean / (checked: boolean, details) => void", description: "Controls checked state." },
  { name: "defaultChecked", type: "boolean", default: "false", description: "Sets the initial uncontrolled state." },
  { name: "indeterminate", type: "boolean", default: "false", description: "Displays partial selection while application state remains authoritative." },
  { name: "wrapperClassName", type: "string", description: "Styles the generated label wrapper when children are present." },
]} />

## Accessibility

- Give every bare Checkbox a specific `aria-label`, such as `Select Ada Lovelace`.
- Group related checkboxes with `fieldset` and `legend`.
- Clear indeterminate state when the controlled collection becomes fully selected or unselected.

<PageNavigation />
