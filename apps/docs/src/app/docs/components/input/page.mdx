import { InputDemo } from "./input-demo";
import { InputAffixesDemo } from "./input-affixes-demo";
import { InputStatesDemo } from "./input-states-demo";

export const metadata = { title: "Input" };
export const usageCode = `<Field>
  <FieldLabel>Email</FieldLabel>
  <Input id="email" type="email" />
</Field>`;

<PageHeader title="Input" description="A single-line text control with three sizes and optional inline prefix and suffix content." />

Compose Input with Field for labels, descriptions, validation, and error messaging. Use Textarea when the value can contain line breaks.

<ComponentPreview label="Labeled input" sourcePath="src/app/docs/components/input/input-demo.tsx"><InputDemo /></ComponentPreview>

## Installation

<Install name="input" />

The copied source is available in the [registry JSON](https://ui.hotfix.jobs/r/input.json). The canonical implementation lives in [packages/react/src/components/input.tsx](https://github.com/hotfix-jobs/patch-ui/blob/main/packages/react/src/components/input.tsx).

## Usage

<Usage code={usageCode} />

For search, compose Input with a search icon in `prefix` and an optional clear action in `suffix`.

## Examples

### Prefix and suffix

Use affixes for supplemental context that is not part of the editable value.

<ComponentPreview label="Prefix and suffix" sourcePath="src/app/docs/components/input/input-affixes-demo.tsx"><InputAffixesDemo /></ComponentPreview>

### Sizes and states

Size controls density. Loading and disabled states preserve the field’s label and value context.

<ComponentPreview label="Sizes and states" sourcePath="src/app/docs/components/input/input-states-demo.tsx"><InputStatesDemo /></ComponentPreview>

## API reference

Input accepts native input attributes. These props add Patch UI behavior.

<PropsTable props={[
  { name: "size", type: "\"sm\" | \"md\" | \"lg\"", default: "\"md\"", description: "Uses the shared 24, 32, or 40 pixel control heights." },
  { name: "variant", type: "\"default\" | \"unstyled\"", default: "\"default\"", description: "Removes control chrome when another component owns the surface." },
  { name: "prefix / suffix", type: "ReactNode / ReactNode", description: "Adds muted inline content before or after the editable value." },
  { name: "loading", type: "boolean", default: "false", description: "Disables the input and replaces the suffix with a spinner." },
]} />

## Accessibility

- Associate every Input with a visible label. A placeholder is not a label.
- Connect descriptions and errors through Field or `aria-describedby`.
- Use prefixes and suffixes for supplemental information, not required instructions.

<PageNavigation />
