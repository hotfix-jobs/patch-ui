import { InputDemo } from "./input-demo";

# Input

A text input aligned with Geist. Three sizes, styled/unstyled `prefix` + `suffix` slots, built-in `label` and `error` message, and a `rounded` (pill) variant. Uses `background-100` fill and translucent borders that step through hover/focus.

<Install name="input" />

<ComponentPreview label="Input Examples">
  <InputDemo />
</ComponentPreview>

## Usage

```tsx
import { Input } from "@patchui/react";
import { Search, Mail, ArrowUpCircle } from "lucide-react";

// Basic
<Input placeholder="Enter text" />

// Sizes
<Input size="sm" placeholder="Small" />   // 32px
<Input size="md" placeholder="Medium" />  // 40px (default)
<Input size="lg" placeholder="Large" />   // 48px

// Prefix and suffix (styled by default — own container + separator)
<Input prefix={<ArrowUpCircle />} placeholder="Default" />
<Input suffix={<ArrowUpCircle />} placeholder="Default" />
<Input prefix="https://" suffix=".com" placeholder="Default" />

// Unstyled prefix/suffix (icons float inside the input area)
<Input prefix={<Search />} prefixStyling={false} placeholder="Search" />

// Inline label and error message
<Input id="domain" label="Domain" placeholder="example.com" />
<Input id="email" label="Email" error="Enter a valid email address." />

// Rounded (pill-shaped)
<Input rounded prefix={<Search />} prefixStyling={false} placeholder="Search" />

// Loading — disables input, shows spinner
<Input loading value={value} />

// For clearable search, use SearchInput (clears on Esc key or Esc chip click)
<SearchInput
  value={query}
  onChange={(e) => setQuery(e.target.value)}
  placeholder="Search projects"
/>
```

## Props

<PropsTable
  props={[
    {
      name: "size",
      type: '"sm" | "md" | "lg"',
      default: '"md"',
      description: "Control height. 32 / 40 / 48px.",
    },
    {
      name: "prefix",
      type: "ReactNode",
      default: "-",
      description: "Content rendered at the start (icon, unit symbol, or text like `https://`).",
    },
    {
      name: "suffix",
      type: "ReactNode",
      default: "-",
      description: "Content rendered at the end (icon, unit label, or text like `.com`). Hidden while loading.",
    },
    {
      name: "prefixStyling",
      type: "boolean",
      default: "true",
      description: "Wraps prefix in a styled container (subtle bg + separator). Set false to float the prefix inside the input area.",
    },
    {
      name: "suffixStyling",
      type: "boolean",
      default: "true",
      description: "Wraps suffix in a styled container (subtle bg + separator). Set false to float the suffix inside the input area.",
    },
    {
      name: "label",
      type: "string",
      default: "-",
      description: "Renders a `<label>` above the input. When set, requires `id`.",
    },
    {
      name: "id",
      type: "string",
      default: "-",
      description: "Required when `label` is a string. Wires up `htmlFor` and `aria-describedby`.",
    },
    {
      name: "error",
      type: "boolean | string",
      default: "false",
      description: "Visual error state. Pass a string to render an inline error message below the input.",
    },
    {
      name: "rounded",
      type: "boolean",
      default: "false",
      description: "Renders a full-radius (pill-shaped) input.",
    },
    {
      name: "loading",
      type: "boolean",
      default: "false",
      description: "Disables the input and renders a small spinner in the trailing slot.",
    },
    {
      name: "disabled",
      type: "boolean",
      default: "false",
      description: "Disables the input.",
    },
    {
      name: "className",
      type: "string",
      default: "-",
      description: "Additional CSS classes applied to the outer wrapper (or the fieldset when `label`/`error` is set).",
    },
  ]}
/>

<Callout title="Best practices">
- **Validate on blur**, not on every keystroke. Surface the message by passing a string to `error`.
- **Trim** leading/trailing whitespace before submit so `example.com` and ` example.com ` resolve to one value.
- **Placeholders** show an example value (`my-awesome-project`, `example.com`), not instructions like `Enter your project name`.
- **Labels** are Title Case nouns: `Project Name`, `Domain`, `Environment Variable Name`.
- **Error messages** name the field and the constraint, end in a period: `Project name is required.`, `Code must be 6 digits.`
- **Prefix / suffix**: styled containers create a distinct visual block for scheme (`https://`) or domain (`.com`). Unstyled (`prefixStyling={false}`) is right for search icons and inline affordances.
- **Icon-only affordances** inside a row of inputs use `<Button shape="circle" aria-label="…">` rather than an unlabeled icon.
</Callout>
