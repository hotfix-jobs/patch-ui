import { ToastActionDemo } from "./toast-action-demo";
import { ToastDemo } from "./toast-demo";
import { ToastTypesDemo } from "./toast-types-demo";

export const metadata = {
  title: "Toast",
  alternates: { canonical: "/docs/components/toast" },
};

export const usageCode = `<>
  <Toaster position="bottom-right" />
  <Button onClick={() => toast.success("Project saved")}>
    Save project
  </Button>
</>`;

<PageHeader
  title="Toast"
  description="A temporary notification that appears above the interface and can be triggered from anywhere."
/>

Use Toast to confirm completed actions, report background work, or surface a brief problem without interrupting the current task.

<ComponentPreview label="Basic toast" sourcePath="src/app/docs/components/toast/toast-demo.tsx">
  <ToastDemo />
</ComponentPreview>

## Installation

<Install name="toast" />

The copied source is available in the [registry JSON](https://ui.hotfix.jobs/r/toast.json). The canonical implementation lives in [packages/react/src/components/toast.tsx](https://github.com/hotfix-jobs/patch-ui/blob/main/packages/react/src/components/toast.tsx).

## Usage

<Usage code={usageCode} />

Mount one Toaster near the application root. Calls to `toast()` and its semantic methods share that global notification queue.

## Examples

### Semantic types

Choose the type that matches the meaning of the message. Loading toasts remain visible until dismissed programmatically.

<ComponentPreview label="Toast types" sourcePath="src/app/docs/components/toast/toast-types-demo.tsx">
  <ToastTypesDemo />
</ComponentPreview>

### Description and action

Add a description when the title needs context. Use one concise action for an immediate response such as Undo.

<ComponentPreview label="Toast with an action" sourcePath="src/app/docs/components/toast/toast-action-demo.tsx">
  <ToastActionDemo />
</ComponentPreview>

## API reference

<PropsTable props={[
  { name: "Toaster.position", type: '"top-left" | "top-center" | "top-right" | "bottom-left" | "bottom-center" | "bottom-right"', default: '"bottom-right"', description: "Places the notification stack against a viewport edge." },
  { name: "Toaster.visibleToasts", type: "number", default: "3", description: "Limits the number of notifications visible in the stack." },
  { name: "Toaster.toastWidth", type: "number", default: "360", description: "Sets the stack width in pixels." },
  { name: "toast options.id", type: "string", description: "Provides an identifier that can update or dismiss a notification." },
  { name: "toast options.description", type: "ReactNode", description: "Adds supporting content below the title." },
  { name: "toast options.duration", type: "number", description: "Overrides the type’s automatic dismissal delay in milliseconds." },
  { name: "toast options.action", type: "{ label: ReactNode; onClick: () => void }", description: "Adds one action that runs and then dismisses the notification." },
]} />

Every toast call returns an ID. Pass it to `toast.dismiss(id)`, or call `toast.dismiss()` without an ID to clear the queue.

## Accessibility

- Success, info, loading, and default notifications announce politely.
- Warning and error notifications use an assertive announcement for higher priority.
- Keep titles brief and avoid placing essential information only in a temporary toast.
- Toasts pause for interaction, support swipe dismissal, and always provide a dismiss button.

<PageNavigation />
