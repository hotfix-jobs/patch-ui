import { DropzoneCompactDemo } from "./dropzone-compact-demo";
import { DropzoneDemo } from "./dropzone-demo";
import { DropzoneSingleDemo } from "./dropzone-single-demo";

export const metadata = { title: "Dropzone" };

export const usageCode = `const [files, setFiles] = useState<File[]>([]);

<Dropzone
  value={files}
  onValueChange={setFiles}
  accept="image/*,application/pdf"
  maxFiles={5}
  maxSize={5 * 1024 * 1024}
/>`;

<PageHeader
  title="Dropzone"
  description="A file-selection surface with drag and drop, native browsing, validation, and removable file rows."
/>

Use Dropzone to collect attachments, images, documents, or imports before handing the selected files to an application-owned upload pipeline.

<ComponentPreview label="File attachments" sourcePath="src/app/docs/blocks/dropzone/dropzone-demo.tsx" resettable>
  <DropzoneDemo />
</ComponentPreview>

## Installation

<Install name="dropzone" />

The copied source is available in the [registry JSON](https://ui.hotfix.jobs/r/dropzone.json). The canonical implementation lives in [packages/react/src/blocks/dropzone/index.tsx](https://github.com/hotfix-jobs/patch-ui/blob/main/packages/react/src/blocks/dropzone/index.tsx).

## Usage

<Usage code={usageCode} />

Dropzone manages file selection, not network transfer. Start the upload from `onValueChange` or a later form submission, and pass `progress` while a transfer is active.

## Examples

### Single file

Set `multiple={false}` for an avatar, resume, or other field that accepts one file.

<ComponentPreview label="Profile image" sourcePath="src/app/docs/blocks/dropzone/dropzone-single-demo.tsx" resettable>
  <DropzoneSingleDemo />
</ComponentPreview>

### Compact

Use the small size when Dropzone sits inside a form, Sheet, or other dense surface. Hide the file list only when the selected file appears elsewhere.

<ComponentPreview label="Compact document upload" sourcePath="src/app/docs/blocks/dropzone/dropzone-compact-demo.tsx">
  <DropzoneCompactDemo />
</ComponentPreview>

## Validation and progress

Use `maxFiles` and `maxSize` for common limits. Supply `validate(file, currentFiles)` when acceptance depends on duplicate names, application rules, or other file metadata.

Pass a number from 0 to 100 to `progress` for determinate upload progress, or `null` for an indeterminate transfer. Selection and removal remain disabled until `progress` becomes `undefined`.

## API reference

<PropsTable props={[
  { name: "value / onValueChange", type: "File[] / (files: File[]) => void", description: "Controls the selected files." },
  { name: "accept", type: "string", description: "Filters the native file picker using MIME types or extensions." },
  { name: "multiple", type: "boolean", default: "true", description: "Allows more than one selected file." },
  { name: "maxFiles / maxSize", type: "number / number", description: "Limits the file count and bytes per file." },
  { name: "validate", type: "(file, currentFiles) => boolean | string", description: "Accepts or rejects each incoming file." },
  { name: "progress", type: "number | null", description: "Displays uploading state across selected file rows." },
  { name: "size", type: '"sm" | "md"', default: '"md"', description: "Selects compact or standalone spacing." },
  { name: "showFileList", type: "boolean", default: "true", description: "Shows removable rows for selected files." },
  { name: "renderFile", type: "(file, index, onRemove) => ReactNode", description: "Replaces the default file row." },
]} />

Title and description accept React nodes so upload requirements can be stated directly in the drop area.

## Accessibility

- The drop area opens the native file picker with Enter or Space.
- State file limits and supported formats in visible helper text.
- Every default file row includes a removal button named with its file name.
- Do not rely on drag and drop as the only selection method.

<PageNavigation />
