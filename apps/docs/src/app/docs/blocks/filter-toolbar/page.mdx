import { FilterToolbarDemo } from "./filter-toolbar-demo";
import { FilterToolbarWrapDemo } from "./filter-toolbar-wrap-demo";

export const metadata = { title: "Filter Toolbar" };

export const usageCode = `<FilterToolbar
  activeCount={activeCount}
  onClearAll={clearFilters}
  count="128 results"
  trailing={<SortMenu />}
>
  <StatusFilter />
  <OwnerFilter />
  <CategoryFilter />
</FilterToolbar>`;

<PageHeader
  title="Filter Toolbar"
  description="A responsive rail for filters, result metadata, clear-all behavior, and pinned trailing actions."
/>

Use Filter Toolbar to organize application-owned filters without coupling their values, popups, or data fetching to the layout block.

<ComponentPreview label="Project filters" sourcePath="src/app/docs/blocks/filter-toolbar/filter-toolbar-demo.tsx" resettable>
  <FilterToolbarDemo />
</ComponentPreview>

## Installation

<Install name="filter-toolbar" />

The copied source is available in the [registry JSON](https://ui.hotfix.jobs/r/filter-toolbar.json). The canonical implementation lives in [packages/react/src/blocks/filter-toolbar/index.tsx](https://github.com/hotfix-jobs/patch-ui/blob/main/packages/react/src/blocks/filter-toolbar/index.tsx).

## Usage

<Usage code={usageCode} />

Pass Menu, Popover, Toggle, or other Patch UI controls as children. Filter Toolbar owns rail layout and stable trailing placement while the application owns filter state.

## Composition

```text
FilterToolbar
├── Filter controls
├── Clear all
├── Result count
└── Trailing actions
```

## Examples

### Wrapping filters

Use wrap on spacious directory or management pages where filters can occupy more than one row. The default scroll behavior is better for headers and narrow screens.

<ComponentPreview label="Wrapping job filters" sourcePath="src/app/docs/blocks/filter-toolbar/filter-toolbar-wrap-demo.tsx" resettable>
  <FilterToolbarWrapDemo />
</ComponentPreview>

## Searchable picker

Filter Toolbar Picker provides the repeated high-cardinality pattern: a searchable, grouped, multi-select Popover. Keep its selection and query controlled so options can be filtered locally or loaded asynchronously.

The trigger summarizes one selection by label and several selections by count. Use `summary` when the product needs different wording.

## API reference

<PropsTable props={[
  { name: "overflow", type: '"scroll" | "wrap"', default: '"scroll"', description: "Keeps one horizontal rail or allows additional rows." },
  { name: "activeCount / onClearAll", type: "number / () => void", default: "0 / undefined", description: "Shows and handles the clear-all action." },
  { name: "count", type: "ReactNode", description: "Adds preformatted result metadata to the trailing region." },
  { name: "countVisibility", type: '"always" | "responsive"', default: '"responsive"', description: "Controls whether count metadata hides below the small breakpoint." },
  { name: "trailing", type: "ReactNode", description: "Pins sorting, view selection, or another action after the rail." },
  { name: "FilterToolbarTrigger.value", type: "ReactNode", description: "Displays an active summary and applies the selected fill." },
  { name: "FilterToolbarPicker.selected / onSelectionChange", type: "string[] / (selected: string[]) => void", description: "Controls the picker’s multi-selection." },
  { name: "FilterToolbarPicker.query / onQueryChange", type: "string / (query: string) => void", description: "Controls local or remote option filtering." },
  { name: "FilterToolbarPicker.sections", type: "FilterToolbarPickerSection[]", description: "Provides grouped options with optional metadata and disabled state." },
]} />

Avoid placeholder choices such as “Any” or “All.” Clear an active filter from its popup, and reserve Clear all for resetting the complete toolbar.

## Accessibility

- Give the toolbar an `ariaLabel` that describes the filter set when “Filters” is too generic.
- Keep result count updates concise because the count uses a polite live region.
- Preserve a visible category label when showing an active filter summary.
- Ensure every custom child control exposes its own name, state, and keyboard behavior.

<PageNavigation />
